<!DOCTYPE html>
<html lang="en">
<head>
  <style>
body{
  color: rgb(52, 54, 47);
  font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

  </style>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <H1>URL Shortener </H1>
    
  <div>
    <form method="Post"  action="/url"><%# method post per /url path se access kar rahe hai (post req hui hai /url pe) %>

      <label for="">Enter Your Original URL </label>
      <input type="text" 
      name="url" <%# ye body.url ki taraha work karega uss post route per  %>
      placeholder="https://example.com">
      <button type="submit">Generate</button>
    </form>
   
  </div>

  <table border="1" cellpadding="10" cellspacing="0">
  <thead>
    <tr>
      <th>S.No</th>
      <th>Short URL</th>
      <th>Redirected URL</th>
      <th>Clicks</th>
    </tr>
  </thead>

  <tbody>
    <% urls.forEach((url, index) => { %>
      <tr>
        <td><%= index + 1 %></td>
        <td><%= url.shortId %></td>
        <td><%= url.redirectURL %></td>
        <td><%= url.visitHistory.length %></td>
      </tr>
    <% }) %>
  </tbody>
</table>

</body>
</html>